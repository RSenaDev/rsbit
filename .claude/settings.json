{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "Set-Location 'C:\\Users\\RS\\claude.test\\rsbit'; git add .; git diff --cached --quiet; if ($LASTEXITCODE -ne 0) { $msg = \"Auto-update $(Get-Date -Format 'yyyy-MM-dd HH:mm')\"; git commit -m $msg; git push origin master }",
            "shell": "powershell",
            "timeout": 60,
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
